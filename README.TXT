Complex weatherApp built with Javascript.

It displays the user current location - call an API after retrieving user's latitude and longitude

It displays the current temperature in C for the desired location(city).
It displays the local time in that specific location.
It displays weather forecats for 3 days in advance considering the day the user makes the searche

3 APIs were called to retrieve the needed data.
